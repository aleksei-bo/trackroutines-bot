package data

var MessageCreateTask = `Please enter task name and points, separated by semi-columns (;), for example: 
Cook dinner; 2

OR use more fields: "Task name; points; periodicity (in days); description"
"Empty garbage bin; 2; 7; take trash to the curb". 
Or send "Cancel" or "No" to abort the task creation.
	`

var MessageUpdateTask = `To update task data, enter `
